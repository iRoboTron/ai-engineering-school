# ИИ для 9 класса

Курс из семи тем о том, как на самом деле устроен искусственный интеллект — для школьников,
которые уже немного программируют. Простым языком, со схемами, повторениями и маленькими
лабораторными на Python. Лабораторные двух уровней: темы 2–6 — офлайн-симуляции на стандартной
библиотеке (запускаются без ключей и интернета), тема 1 — ноутбуки в Google Colab с настоящими
инструментами: токенизатором `tiktoken` от OpenAI и разговором с реальной моделью через
школьный сервер.

Сделан на основе взрослого курса `../ai-engineering` (интенсив для разработчиков): темы те же,
изложение и глубина — школьные.

## Программа

| Тема | О чём | Уроков |
|---|---|---|
| 1 | Как устроена языковая модель: токены, вероятности, температура, галлюцинации, инструменты | 3 |
| 2 | RAG: как научить ИИ отвечать по своим документам | 2 |
| 3 | Векторы: поиск по смыслу и почему перебор не годится для больших данных | 2 |
| 4 | Агенты: цикл действий, инструменты и границы безопасности | 2 |
| 5 | Проверка качества: эталонный набор, ИИ-судья, тихая деградация | 2 |
| 6 | Модели и безопасность: облако против своего компьютера, подмена инструкций | 2 |
| 7 | Свой проект и как о нём честно рассказать | 2 |

В каждой теме есть словарик терминов со схемами.

## Как открыть локально

```bash
cd docs/books
python3 -m http.server 8099 --bind 127.0.0.1
```

Открыть http://127.0.0.1:8099/index.html — это каталог тем. Читалка (`reader.html`) собирает
всю тему в одну страницу: можно читать подряд, переходить по оглавлению и печатать в PDF.

Всё работает офлайн: библиотеки (marked, highlight.js, mermaid, DOMPurify) лежат в `vendor/`,
внешних CDN нет.

## Лабораторные

```bash
python3 docs/books/labs/tema1-llm-osnovy/01_tokeny.py
```

Лабораторные двух уровней.

**Офлайн-симуляции.** Запускаются одной командой, используют только стандартную
библиотеку, ничего никуда не отправляют и не требуют установки. Они объясняют механизм:
вместо настоящей модели — простая функция-заглушка, чтобы был виден сам механизм, а не
работа чужой библиотеки.

| Файл | Что показывает |
|---|---|
| `tema1-llm-osnovy/01_tokeny.py` | Как текст режется на токены и почему русский «дороже» |
| `tema1-llm-osnovy/02_temperatura.py` | Крошечная языковая модель и влияние температуры |
| `tema1-llm-osnovy/03_instrumenty.py` | Цикл «модель просит инструмент — программа выполняет» |
| `tema2-rag/01_mini_rag.py` | Мини-RAG: чанки, поиск по словам, сборка запроса |
| `tema3-vektory/01_poisk_po_smyslu.py` | Поиск по смыслу через косинусную близость |
| `tema4-agenty/01_agent.py` | Агент с двумя инструментами и четырьмя границами |
| `tema5-provierka/01_proverka.py` | Замер качества на эталонном наборе, до и после улучшения |
| `tema6-bezopasnost/01_podmena.py` | Как выглядит подмена инструкций и как от неё защищаются |

**Настоящие инструменты (тема 1).** Ноутбуки `.ipynb` темы 1 подключают реальные
библиотеки: токенизатор `tiktoken` от OpenAI и разговор с настоящей моделью через
официальный клиент `openai`. Модель отвечает через школьный сервер: ключ хранится
только на сервере, ученик использует «код класса» от учителя. Открывать ноутбуки
проще всего в Google Colab — устанавливать ничего не нужно:

- [Лаборатория 1.1 — настоящий токенизатор (Colab)](https://colab.research.google.com/github/iRoboTron/ai-engineering-school/blob/main/docs/books/labs/tema1-llm-osnovy/01_tokeny.ipynb)
- [Лаборатория 1.2 — первый разговор с настоящей моделью (Colab)](https://colab.research.google.com/github/iRoboTron/ai-engineering-school/blob/main/docs/books/labs/tema1-llm-osnovy/02_generaciya.ipynb)

Исходники ноутбуков лежат в `notebooks/` как обычные `.py`, из которых они собраны
(см. «Проверка» и `scripts/build_notebooks.py`).

Код в уроке и код в файле лабораторной — одно и то же: в уроке он вставлен, чтобы читать
не отходя, а файл нужен, чтобы запустить и поменять.

## Структура

```
docs/books/
├── index.html          каталог тем
├── reader.html         читалка
├── files.json          манифест: какие темы и уроки существуют
├── NN-slug/            тема: book.md, chapter-NN.md, glossary.md
├── labs/temaN-slug/    лабораторные: .py (офлайн) и .ipynb (ноутбуки)
└── vendor/             офлайн-библиотеки

notebooks/              исходники .ipynb-ноутбуков как обычные .py
```

Правила написания уроков — в [AUTHORING-CONVENTIONS.md](AUTHORING-CONVENTIONS.md).

## Проверка

```bash
python3 scripts/check_project.py
python3 scripts/build_notebooks.py --check   # ноутбуки в репозитории не устарели
```

`check_project.py` проверяет офлайн, без сети и зависимостей: структура и `files.json`, битые
ссылки, тип и палитра схем, наличие определений и разделов «Что мы узнали» / «Проверь себя»,
совпадение версии ассетов в трёх местах, совпадение кода в уроке с файлом лабораторной,
запуск всех лабораторных и то, что на каждый `.ipynb` ссылается хотя бы один урок.
`build_notebooks.py --check` сверяет собранные ноутбуки с исходниками в `notebooks/`.

PASS означает согласованность материалов, а не педагогическое качество: уроки всё равно нужно
посмотреть глазами в браузере.

При изменении контента поменяй `ASSET_VERSION` в `index.html` и `reader.html` (и в теге
`reader-links.js?v=` — три места должны совпадать), иначе браузер отдаст старую версию из кэша.
`check_project.py` падает, если версии разошлись.

## Публикация

Сайт: **https://ai9.adelfos.ru** — контейнер CT 109 на Proxmox `pxhome`, nginx, статика.

```bash
./deploy-pxhome.sh              # проверки + сборка + выкладка (через ssh-алиас pxhome)
./deploy-pxhome.sh --build-only # только собрать: BUILD_DIR=/tmp/site ./deploy-pxhome.sh --build-only
```

Как устроено: сайт — это ровно содержимое `docs/books`. Скрипт распаковывает релиз в
`/var/www/releases/<домен>/<дата>`, пишет vhost, атомарно переключает симлинк
`/var/www/sites/<домен>` и перезагружает nginx. Дальше — дымовые проверки (7 ключевых файлов
должны отдавать 200). Если что-то пошло не так, предыдущий релиз и vhost возвращаются
автоматически. Хранятся последние 5 релизов.

Переменные: `SITE_DIR` (домен), `CTID`, `VIA` (`host` — через хост и `pct`, по умолчанию;
`direct` — ssh прямо в контейнер, так работает CI), `CHECK=0` — пропустить проверки.

## CI/CD

| Событие | Что происходит | Где выполняется |
|---|---|---|
| push в ветку, pull request | `check_project.py` + синтаксис скриптов | облачный раннер GitHub |
| push в `main` | те же проверки, затем автоматическая выкладка | проверки — GitHub, выкладка — свой раннер в CT 121 |

Публиковать с облачного раннера нельзя: Proxmox не виден из интернета. Поэтому job `deploy`
выполняется на self-hosted раннере с меткой `ai9`, который живёт в CT 121 и ходит по LAN прямо
в CT 109.

Настройка автодеплоя (один раз, со своей машины — нужны `ssh pxhome` и авторизованный `gh`):

```bash
./scripts/setup-ci-runner.sh
```

Скрипт добавляет ssh-ключ раннера в CT 109, ставит в CT 121 второй экземпляр GitHub-раннера
(отдельный каталог `/opt/actions-runner-ai9`, отдельный сервис — существующий раннер репозитория
`web-agent` не трогается), регистрирует его на этот репозиторий с меткой `ai9` и запускает.
Повторный запуск безопасен.

Проверить раннеры: `gh api repos/iRoboTron/ai-engineering-school/actions/runners -q '.runners[].name'`

### Внешний доступ (HTTPS)

DNS на `*.adelfos.ru` уже указывает на нужный IP. Что нужно сделать в **Nginx Proxy Manager**
(CT 101) один раз:

1. Proxy Hosts → Add Proxy Host.
2. Domain Names: `ai9.adelfos.ru`; Scheme `http`, Forward Hostname `192.168.0.109`, Port `80`.
3. Включить Block Common Exploits и Websockets Support.
4. Вкладка SSL: Request a new SSL Certificate, Force SSL, HTTP/2, согласиться с Let's Encrypt.

Пока proxy host не создан, `deploy-pxhome.sh` выкладывает сайт, но честно пишет, что снаружи
он ещё недоступен.
